# 视频生成功能手动测试指南

## 测试目标
验证 SORA 2.0 和 VEO 3.1 两个模型是否能正常生成视频

## 前置条件
1. ✅ 应用已启动（`npm run dev`）
2. ✅ 数据库连接正常
3. ✅ 已配置 SUCHUANG_API_KEY 环境变量
4. ✅ 已注册用户账号并登录
5. ✅ 账号有足够的积分（至少15积分）

## 测试步骤

### 测试 1: SORA 2.0 模型

#### 1.1 访问生成页面
- 打开浏览器访问：`http://localhost:3000/generate`
- 或从首页点击"立即开始创作"

#### 1.2 选择 SORA 2.0 模型
- 在"选择AI模型"区域，点击 **SORA 2.0** 卡片
- 确认卡片有黄色边框和勾选图标
- 确认显示"推荐"标签

#### 1.3 输入测试内容
```
提示词：一只可爱的小猫在草地上玩耍，阳光明媚，卡通风格，色彩鲜艳
时长：10秒
比例：9:16
清晰度：标清
```

#### 1.4 生成视频
- 点击"开始生成视频"按钮
- 观察页面显示"生成中"状态
- 等待 30-60 秒

#### 1.5 验证结果
**预期结果：**
- ✅ 视频生成成功
- ✅ 可以播放视频
- ✅ 视频时长约 10 秒
- ✅ 视频比例为 9:16（竖屏）
- ✅ 积分正确扣除（10积分）

**如果失败：**
- ❌ 检查浏览器控制台错误信息
- ❌ 检查网络请求（F12 -> Network）
- ❌ 查看服务器日志

---

### 测试 2: VEO 3.1 模型

#### 2.1 刷新页面或清空输入
- 刷新页面或点击"重新生成"

#### 2.2 选择 VEO 3.1 模型
- 在"选择AI模型"区域，点击 **VEO 3.1** 卡片
- 确认卡片有黄色边框和勾选图标
- 确认显示"专业模型"标签

#### 2.3 输入测试内容
```
提示词：城市夜景，霓虹灯闪烁，车流穿梭，现代都市氛围
比例：16:9
```

#### 2.4 生成视频
- 点击"开始生成视频"按钮
- 观察页面显示"生成中"状态
- 等待 30-60 秒

#### 2.5 验证结果
**预期结果：**
- ✅ 视频生成成功
- ✅ 可以播放视频
- ✅ 视频时长约 5 秒
- ✅ 视频比例为 16:9（横屏）
- ✅ 积分正确扣除（15积分）

---

### 测试 3: 带图片的生成（可选）

#### 3.1 选择任一模型
- 选择 SORA 2.0 或 VEO 3.1

#### 3.2 上传参考图片
- 点击"参考图片"区域的上传按钮
- 选择 1-2 张图片（JPG/PNG，每张<5MB）

#### 3.3 输入提示词并生成
```
提示词：基于参考图片的风格，创作一个动态场景
```

#### 3.4 验证结果
**预期结果：**
- ✅ 视频生成成功
- ✅ 视频风格与参考图片相关
- ✅ 积分正确扣除（基础积分 + 图片积分）

---

### 测试 4: 首页快速生成

#### 4.1 访问首页
- 打开：`http://localhost:3000/`
- 滚动到"创作工具"区域

#### 4.2 在首页生成视频
- 选择模型（SORA 2.0 或 VEO 3.1）
- 输入提示词
- 点击"开始生成视频"

#### 4.3 验证结果
**预期结果：**
- ✅ 首页生成功能正常
- ✅ 视频在右侧预览区域显示
- ✅ 可以下载视频

---

## 常见问题排查

### 问题 1: 积分不足
**症状：** 点击生成按钮无反应或提示积分不足

**解决方案：**
1. 检查积分余额（页面右上角或个人中心）
2. 如果是测试环境，可以直接在数据库中增加积分：
```sql
UPDATE user_credit_accounts 
SET available_credits = available_credits + 100 
WHERE user_id = (SELECT id FROM users WHERE email = 'your-email@example.com');
```

### 问题 2: API 调用失败
**症状：** 提示"生成失败"或"API错误"

**解决方案：**
1. 检查 `.env` 文件中的 `SUCHUANG_API_KEY` 是否正确
2. 检查 API 服务商账户余额是否充足
3. 查看服务器日志：
```bash
# 查看 Next.js 开发服务器输出
# 应该能看到 API 调用的详细日志
```

### 问题 3: 视频生成超时
**症状：** 等待超过 2 分钟仍未完成

**解决方案：**
1. 检查网络连接
2. 查看数据库中的视频记录状态：
```sql
SELECT id, status, error_message, created_at 
FROM video_generations 
ORDER BY created_at DESC 
LIMIT 5;
```
3. 如果状态为 PROCESSING，可能是 API 服务商响应慢，继续等待
4. 如果状态为 FAILED，查看 error_message

### 问题 4: 视频无法播放
**症状：** 视频生成成功但无法播放

**解决方案：**
1. 检查视频 URL 是否有效（在浏览器中直接访问）
2. 检查视频格式是否为 MP4
3. 尝试在不同浏览器中播放

---

## 自动化测试脚本

如果你想运行自动化测试，可以使用提供的测试脚本：

### 步骤 1: 获取 Session Cookie
1. 在浏览器中登录网站
2. 打开开发者工具（F12）
3. 切换到 Application/Storage 标签
4. 找到 Cookies
5. 复制完整的 Cookie 字符串

### 步骤 2: 配置测试脚本
编辑 `test-video-generation.js`：
```javascript
const TEST_CONFIG = {
  sessionCookie: '你的Cookie字符串',
  // ...
}
```

### 步骤 3: 运行测试
```bash
node test-video-generation.js
```

---

## 测试检查清单

### 功能测试
- [ ] SORA 2.0 模型可以正常生成视频
- [ ] VEO 3.1 模型可以正常生成视频
- [ ] 模型切换功能正常
- [ ] 积分扣除正确
- [ ] 视频可以正常播放
- [ ] 视频可以下载

### UI 测试
- [ ] 模型卡片显示正确的标签
- [ ] 选中状态显示正确
- [ ] 积分余额显示正确
- [ ] 积分不足时有警告提示
- [ ] 生成中状态显示正确
- [ ] 生成完成后视频正确显示

### 边界测试
- [ ] 积分不足时无法生成
- [ ] 提示词为空时无法生成
- [ ] 提示词超长时有限制
- [ ] 图片格式不正确时有提示
- [ ] 图片过大时有提示

---

## 测试报告模板

```
测试日期：____________________
测试人员：____________________
环境：开发环境 / 生产环境

测试结果：
✅ SORA 2.0 模型：通过 / 失败
✅ VEO 3.1 模型：通过 / 失败
✅ 首页生成功能：通过 / 失败
✅ 带图片生成：通过 / 失败

问题记录：
1. ___________________________________
2. ___________________________________
3. ___________________________________

备注：
_______________________________________
_______________________________________
```

---

## 下一步

测试通过后，可以：
1. 部署到生产环境
2. 邀请用户进行 Beta 测试
3. 监控生产环境的视频生成成功率
4. 收集用户反馈并优化
