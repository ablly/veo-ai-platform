# 📱 腾讯云短信服务配置指南

> **VEO AI 生产环境 - 完整配置文档**
> 
> 最后更新：2025-01-XX
> 
> 本文档将指导您完成腾讯云短信服务的完整配置，确保达到生产环境标准。

---

## 📋 目录

1. [前置准备](#前置准备)
2. [腾讯云控制台配置](#腾讯云控制台配置)
3. [获取配置参数](#获取配置参数)
4. [环境变量配置](#环境变量配置)
5. [数据库迁移](#数据库迁移)
6. [EdgeOne部署配置](#edgeone部署配置)
7. [测试验证](#测试验证)
8. [故障排查](#故障排查)
9. [成本优化](#成本优化)

---

## 🎯 前置准备

### 必备条件

- ✅ 腾讯云账号（已实名认证）
- ✅ 企业营业执照或个人身份证
- ✅ 网站ICP备案（中国大陆）
- ✅ 短信签名（您的：**成都喜悦网络科技有限公司**）
- ✅ 充值余额（建议至少100元）

### 当前状态检查

**您的签名状态：**
```
签名内容：成都喜悦网络科技有限公司
签名状态：报备中（等待工信部审核）
预计时间：1-3个工作日
```

**说明：** 签名审核通过后，代码无需修改，立即生效！

---

## 🔧 腾讯云控制台配置

### 步骤1：访问短信控制台

**控制台地址：** https://console.cloud.tencent.com/smsv2

登录后，您应该看到以下界面：
- 左侧菜单：国内短信、应用管理、签名管理、正文模板管理
- 右侧显示：套餐包余额、本月使用情况

---

### 步骤2：创建访问密钥（SecretId & SecretKey）

#### 2.1 访问密钥管理页面

**地址：** https://console.cloud.tencent.com/cam/capi

#### 2.2 创建新密钥

1. 点击"新建密钥"按钮
2. 系统会生成一对密钥：
   - **SecretId**: 以 `AKID` 开头的字符串
   - **SecretKey**: 随机字符串

⚠️ **重要：** SecretKey 只显示一次，请立即保存！

#### 2.3 保存密钥

```text
SecretId: AKID********************************
SecretKey: ********************************
```

**安全建议：**
- ❌ 不要提交到Git仓库
- ❌ 不要分享给他人
- ✅ 保存到密码管理器
- ✅ 定期轮换密钥

---

### 步骤3：获取应用ID (SDKAppID)

#### 3.1 访问应用管理

**路径：** 短信控制台 → 应用管理

#### 3.2 查看或创建应用

如果没有应用，点击"创建应用"：
- 应用名称：VEO AI（或您喜欢的名称）
- 应用简介：VEO AI视频创作平台

创建后，您会获得：
```text
应用ID (SDKAppID): 1400******
```

**示例：** `1400123456`

---

### 步骤4：确认签名状态

#### 4.1 访问签名管理

**路径：** 短信控制台 → 国内短信 → 签名管理

#### 4.2 查看您的签名

您应该看到：
```
签名内容：成都喜悦网络科技有限公司
签名用途：自用
签名类型：公司
状态：报备中 / 已通过
```

**如果状态是"报备中"：**
- ⏳ 等待1-3个工作日
- 📧 审核结果会发送到您的邮箱
- ✅ 审核通过后，无需修改代码

**如果状态是"未通过"：**
- 查看拒绝原因
- 修改后重新提交
- 或联系腾讯云客服

---

### 步骤5：创建短信模板

#### 5.1 访问模板管理

**路径：** 短信控制台 → 国内短信 → 正文模板管理

#### 5.2 创建验证码模板

点击"创建正文模板"：

**模板信息：**
```
模板名称：登录验证码
短信类型：验证码短信
短信内容：您的验证码是{1}，{2}分钟内有效。若非本人操作，请忽略此短信。
申请说明：用于用户登录验证
```

**参数说明：**
- `{1}` - 验证码（6位数字）
- `{2}` - 有效期（5分钟）

#### 5.3 等待审核

审核时间：通常2-4小时

审核通过后，您会获得：
```text
模板ID: 12345
```

**记下这个模板ID，稍后需要配置！**

---

## 📦 获取配置参数

完成以上步骤后，您应该拥有以下5个配置参数：

| 参数名称 | 示例值 | 获取路径 |
|---------|--------|---------|
| **SecretId** | `AKID********************************` | 访问管理 → 访问密钥 |
| **SecretKey** | `********************************` | 访问管理 → 访问密钥 |
| **SDKAppID** | `1400123456` | 短信控制台 → 应用管理 |
| **签名内容** | `成都喜悦网络科技有限公司` | 短信控制台 → 签名管理 |
| **模板ID** | `12345` | 短信控制台 → 正文模板管理 |

⚠️ **请确保所有参数都已获取，否则短信无法发送！**

---

## ⚙️ 环境变量配置

### 本地开发环境配置

#### 编辑 `.env` 文件

**文件路径：** `Z:\appdemo\app\my\test1\veo-ai-platform\.env`

**添加以下配置：**
```env
# ====================================
# 腾讯云短信配置（生产环境）
# ====================================

# 腾讯云访问密钥ID（从访问管理获取）
TENCENT_SMS_SECRET_ID=AKID********************************

# 腾讯云访问密钥Key（从访问管理获取）
TENCENT_SMS_SECRET_KEY=********************************

# 短信应用ID（从短信控制台-应用管理获取）
TENCENT_SMS_SDK_APP_ID=1400******

# 短信签名（从短信控制台-签名管理获取）
TENCENT_SMS_SIGN_NAME=成都喜悦网络科技有限公司

# 短信模板ID（从短信控制台-正文模板管理获取）
TENCENT_SMS_TEMPLATE_ID=*****

# ====================================
# 短信发送限制配置（可选）
# ====================================

# 单个手机号每日发送上限（防止恶意刷短信）
SMS_DAILY_LIMIT_PER_PHONE=10
```

**配置说明：**
- 将示例值替换为您从腾讯云获取的实际值
- 确保没有多余的空格或引号
- 保存文件后重启开发服务器

---

### 生产环境配置（EdgeOne）

#### 使用EdgeOne环境变量

生产环境不使用 `.env` 文件，需要在EdgeOne控制台配置。

**配置步骤：**

1. 登录EdgeOne控制台
2. 进入您的应用
3. 点击"环境变量"
4. 添加以下变量：

| 变量名 | 变量值 | 说明 |
|--------|--------|------|
| `TENCENT_SMS_SECRET_ID` | `AKID***` | 腾讯云访问密钥ID |
| `TENCENT_SMS_SECRET_KEY` | `***` | 腾讯云访问密钥Key |
| `TENCENT_SMS_SDK_APP_ID` | `1400***` | 短信应用ID |
| `TENCENT_SMS_SIGN_NAME` | `成都喜悦网络科技有限公司` | 短信签名 |
| `TENCENT_SMS_TEMPLATE_ID` | `***` | 短信模板ID |
| `SMS_DAILY_LIMIT_PER_PHONE` | `10` | 每日发送限制 |

5. 保存并重新部署

---

## 🗄️ 数据库迁移

### 执行数据库迁移脚本

**文件路径：** `database-phone-sms-enhancement.sql`

#### 方法1：使用数据库客户端

```sql
-- 连接到您的PostgreSQL数据库
psql -h YOUR_DB_HOST -U YOUR_DB_USER -d YOUR_DB_NAME

-- 执行迁移脚本
\i database-phone-sms-enhancement.sql
```

#### 方法2：直接执行SQL

复制 `database-phone-sms-enhancement.sql` 文件内容，在数据库管理工具中执行。

#### 验证迁移成功

```sql
-- 检查字段是否添加成功
\d phone_verification_codes

-- 应该看到新字段：
-- - send_status
-- - send_error
-- - tencent_request_id
-- - send_attempts
```

---

## 🚀 EdgeOne部署配置

### 部署步骤

#### 1. 提交代码

```bash
git add .
git commit -m "feat: 集成腾讯云短信服务（生产环境）"
git push origin main
```

#### 2. 配置环境变量

参考上面的"生产环境配置（EdgeOne）"部分

#### 3. 触发部署

EdgeOne会自动检测代码变更并开始部署。

#### 4. 等待部署完成

通常需要3-5分钟。

---

## ✅ 测试验证

### 测试清单

#### 1. 本地开发环境测试

```bash
# 启动开发服务器
npm run dev

# 访问登录页面
http://localhost:3000/login

# 测试步骤：
1. 点击"手机验证码"标签
2. 输入您的手机号
3. 点击"获取验证码"
4. 检查是否收到短信
5. 输入验证码登录
```

**预期结果：**
- ✅ 收到短信验证码（6位数字）
- ✅ 短信内容符合模板
- ✅ 签名显示为"成都喜悦网络科技有限公司"
- ✅ 验证码可以成功登录

---

#### 2. 生产环境测试

```bash
# 访问线上地址
https://www.veo-ai.site/login

# 测试步骤相同
```

---

#### 3. 检查日志

**查看服务器日志：**
```bash
# 查看短信发送日志
# 成功日志示例：
✅ 短信发送成功 - 手机号: 138****8888, 请求ID: xxx, 费用: 0.045元

# 失败日志示例：
❌ 短信发送失败: { errorCode: 'xxx', errorMessage: 'xxx' }
```

---

#### 4. 检查数据库

```sql
-- 查看最近的短信发送记录
SELECT * FROM recent_sms_logs LIMIT 10;

-- 查看发送统计
SELECT * FROM sms_statistics;

-- 查看失败记录
SELECT * FROM phone_verification_codes 
WHERE send_status = 'failed' 
ORDER BY created_at DESC 
LIMIT 5;
```

---

## 🔍 故障排查

### 常见问题

#### 问题1：签名未审核通过

**错误代码：** `FailedOperation.SignatureIncorrectOrUnapproved`

**原因：**
- 签名还在"报备中"状态
- 签名审核被拒绝

**解决方案：**
1. 登录腾讯云控制台检查签名状态
2. 如果被拒绝，查看拒绝原因并修改
3. 等待审核通过（1-3个工作日）

---

#### 问题2：模板未审核通过

**错误代码：** `FailedOperation.TemplateIncorrectOrUnapproved`

**原因：**
- 模板还在审核中
- 模板审核被拒绝

**解决方案：**
1. 检查模板状态
2. 修改模板内容重新提交
3. 确保模板内容符合规范

---

#### 问题3：余额不足

**错误代码：** `FailedOperation.InsufficientBalanceInSmsPackage`

**原因：**
- 短信套餐包余额不足
- 账户欠费

**解决方案：**
1. 登录腾讯云充值
2. 建议充值至少100元
3. 设置余额告警

---

#### 问题4：SecretId配置错误

**错误代码：** `AuthFailure.SecretIdNotFound`

**原因：**
- `TENCENT_SMS_SECRET_ID` 配置错误
- `TENCENT_SMS_SECRET_KEY` 配置错误

**解决方案：**
1. 检查环境变量配置
2. 确保没有多余的空格或引号
3. 重新获取密钥

---

#### 问题5：发送频率过高

**错误代码：** `LimitExceeded.PhoneNumberDailyLimit`

**原因：**
- 单个手机号发送次数超过限制

**解决方案：**
- 这是正常的安全防护
- 等待24小时后重试
- 或调整 `SMS_DAILY_LIMIT_PER_PHONE` 配置

---

### 查看详细错误日志

**服务器日志：**
```bash
# 查看最近的错误日志
pm2 logs --lines 100 --err
```

**数据库日志：**
```sql
-- 查看失败的短信记录
SELECT 
    phone,
    send_error,
    tencent_request_id,
    created_at
FROM phone_verification_codes
WHERE send_status = 'failed'
ORDER BY created_at DESC
LIMIT 20;
```

---

## 💰 成本优化

### 短信费用

**腾讯云短信价格：**
- 中国大陆：0.045元/条
- 验证码短信：约0.055元/条（包含签名）

### 每月成本估算

**场景1：小型项目**
```
每日登录用户：100人
每月短信量：3000条
每月费用：3000 × 0.045 = 135元
```

**场景2：中型项目**
```
每日登录用户：500人
每月短信量：15000条
每月费用：15000 × 0.045 = 675元
```

**场景3：大型项目**
```
每日登录用户：2000人
每月短信量：60000条
每月费用：60000 × 0.045 = 2700元
```

### 成本控制措施

#### 1. 频率限制

已在代码中实现：
- 1分钟内只能发送1次
- 每日最多发送10次（可配置）

#### 2. 验证码有效期

- 5分钟有效期（避免重复发送）

#### 3. 监控告警

- 每日发送量监控
- 异常发送告警（发送到管理员邮箱）

#### 4. 购买套餐包

腾讯云提供优惠套餐包：
- 1000条：40元（0.04元/条）
- 10000条：360元（0.036元/条）
- 100000条：2900元（0.029元/条）

**建议：** 根据实际使用量购买合适的套餐包

---

## 📊 监控和统计

### 查看发送统计

```sql
-- 查看每日发送统计
SELECT * FROM sms_statistics;

-- 查看成功率
SELECT 
    COUNT(*) FILTER (WHERE send_status = 'sent') as success_count,
    COUNT(*) FILTER (WHERE send_status = 'failed') as failed_count,
    ROUND(
        COUNT(*) FILTER (WHERE send_status = 'sent')::NUMERIC / 
        COUNT(*) * 100, 
        2
    ) as success_rate
FROM phone_verification_codes
WHERE created_at > NOW() - INTERVAL '7 days';
```

### 设置余额告警

**腾讯云控制台：**
1. 进入"费用中心"
2. 设置"余额告警"
3. 余额低于100元时发送邮件

---

## ✅ 生产环境上线检查清单

在正式上线前，请确认以下所有项目：

- [ ] ✅ 签名已审核通过
- [ ] ✅ 模板已审核通过
- [ ] ✅ 环境变量已正确配置（EdgeOne）
- [ ] ✅ 数据库迁移已执行
- [ ] ✅ 短信余额充足（建议至少100元）
- [ ] ✅ 本地测试通过（收到短信）
- [ ] ✅ 生产环境测试通过
- [ ] ✅ 日志记录正常
- [ ] ✅ 管理员告警邮件配置正确
- [ ] ✅ 频率限制生效
- [ ] ✅ 成本监控已设置

---

## 📞 技术支持

### 腾讯云客服

- **电话：** 95716
- **工作时间：** 7×24小时
- **在线客服：** https://console.cloud.tencent.com/workorder

### 常用链接

- **短信控制台：** https://console.cloud.tencent.com/smsv2
- **API文档：** https://cloud.tencent.com/document/product/382
- **SDK文档：** https://cloud.tencent.com/document/sdk/Node.js
- **错误码：** https://cloud.tencent.com/document/product/382/38774

---

## 🎉 恭喜！

您已完成腾讯云短信服务的完整配置！

**下一步：**
1. 等待签名审核通过（1-3个工作日）
2. 审核通过后，立即测试
3. 监控短信发送情况
4. 根据实际使用量优化成本

**如有问题，请查看故障排查部分或联系技术支持。** 🚀












